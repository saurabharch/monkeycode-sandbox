{
  "name": "monkeycode-sandbox",
  "version": "0.1.0",
  "description": "Isolated sandbox codebase for MonkeyCode sidecar demo coding tasks",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^3.2.6"
  }
}
